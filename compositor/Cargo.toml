[package]
name = "planktop"
version = "0.1.0"
edition = "2021"
description = "openPlank Wayland Compositor — KI-gesteuert"
license = "MIT"
repository = "https://github.com/openplank-dev/openplank-os"

# Phase 2 dependencies (uncomment when implementing)
# [dependencies]
# smithay = "0.3"
# wayland-server = "0.31"
# calloop = "0.13"
# tracing = "0.1"
# tracing-subscriber = "0.3"
# dbus = "0.9"
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
